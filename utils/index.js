const writeToFile = require('./writeToFile')
const saveJSON = require('./saveJSON')
const cachedUrl = require('./cachedUrl')
const log = require('./log')
const zhDigit2Arabic = require('./zhDigit2Arabic')

module.exports = {
  writeToFile,
  cachedUrl,
  log,
  zhDigit2Arabic,
  saveJSON
}
